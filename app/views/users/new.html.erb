<%= form_with url: '/users', model: User.new, local: true do |f| %>
  <%= f.label "Username:" %>
  <%= f.text_field :username %>

  <%= f.label "Email:" %>
  <%= f.email_field :email %>

  <%= f.label "New Password:" %>
  <%= f.password_field :password %>

  <%= f.submit "Register" %>
<% end %>
